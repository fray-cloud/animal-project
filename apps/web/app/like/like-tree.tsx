'use client';

import LikePage from 'front/new-site/like/LikePage';
import RouteShell from '../route-shell';

export default function LikeTree() {
  return (
    <RouteShell>
      <LikePage />
    </RouteShell>
  );
}
