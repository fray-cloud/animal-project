// @ts-check
// Scaffolded for #29 of the Vite → Next.js migration (#7).
// The Nx plugin registration and the actual routing cutover land in #31.

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = nextConfig;
